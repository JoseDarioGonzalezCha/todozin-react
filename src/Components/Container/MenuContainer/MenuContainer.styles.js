import { styled } from "styled-components";

export const TagMenuContainer = styled.aside`
  background-color: ${({ theme }) => theme.colors.bgMenu};
  border-radius: 0.9375rem;
  height: 100vh;

  padding: 2.5rem 1.25rem;
  margin: 10px;
`;
